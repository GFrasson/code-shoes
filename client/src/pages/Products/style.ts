import styled from 'styled-components';

export const ProductsContainer = styled.div`
    .products-page {
        margin: 4rem 6.25rem;

        .search {
            padding: 0.5rem;
            border-radius: 1.25rem;
        }
    }
`;